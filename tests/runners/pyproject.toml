[project]
name = "nextdftb-tests"
version = "0.0.0"
description = "External test runners for nextdftb (pytest-based)"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8",
    "numpy>=1.24",
    "h5py>=3.9",
]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["."]
